<UserControl x:Class="CadEye.View.Cad_Text_History"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CadEye.View"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Border Grid.Row="0" Background="#444444" CornerRadius="5" HorizontalAlignment="Stretch" Height="25" Padding="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                </Grid.ColumnDefinitions>
                <Button 
                        FontFamily="Segoe MDL2 Assets" 
                        Grid.Column="1" Content="&#xE721;" 
                        HorizontalAlignment="Right" 
                        Background="Transparent"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center" 
                        FontSize="13" Foreground="White" 
                        BorderThickness="0" />
                <TextBox 
                        x:Name="Tag_SearchText" 
                        Grid.Column="3" 
                        TextChanged="Overlay_Tag" 
                        Style="{StaticResource SearchTextBoxStyle}">
                    <TextBox.InputBindings>
                        <KeyBinding 
                                Key="Enter" 
                                Command="{Binding DataContext.History_Tag_Enter_Search_Command, RelativeSource={RelativeSource AncestorType=TextBox}}" 
                                CommandParameter="{Binding Text, RelativeSource={RelativeSource AncestorType=TextBox}}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <TextBox 
                        x:Name="PlaceHolder_Overlay_Tag" 
                        Text="Tag" 
                        Grid.Column="3" 
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        Style="{StaticResource SearchTextBoxStyle}"
                     IsReadOnly="True"/>
            </Grid>
        </Border>


        <Border Grid.Row="1" Background="#444444" CornerRadius="5" HorizontalAlignment="Stretch" Height="25" Padding="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                </Grid.ColumnDefinitions>
                <Button 
                        FontFamily="Segoe MDL2 Assets" 
                        Grid.Column="1" 
                        Content="&#xE721;" 
                        HorizontalAlignment="Right" 
                        Background="Transparent"
                        HorizontalContentAlignment="Center" 
                        VerticalContentAlignment="Center" 
                        FontSize="13" 
                        Foreground="White" 
                        BorderThickness="0" />
                <TextBox 
                        x:Name="File_SearchText" 
                        Grid.Column="3" 
                        TextChanged="Overlay_FileName" 
                        Style="{StaticResource SearchTextBoxStyle}">
                    <TextBox.InputBindings>
                        <KeyBinding 
                                Key="Enter" 
                                Command="{Binding DataContext.History_Ref_Enter_Search_Command, RelativeSource={RelativeSource AncestorType=TextBox}}" 
                                CommandParameter="{Binding Text, RelativeSource={RelativeSource AncestorType=TextBox}}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <TextBox 
                        x:Name="PlaceHolder_Overlay_FileName" 
                        Text="File Name" 
                        Grid.Column="3" 
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Left" 
                        Style="{StaticResource SearchTextBoxStyle}"
                    IsReadOnly="True" />
            </Grid>
        </Border>


        <Label Grid.Row="2" Style="{StaticResource Body-Label}"
        Content="T A G"></Label>
        <Border Grid.Row="3" >
            <ListBox x:Name="Note" ItemsSource="{Binding History_Tag}" SelectionMode="Multiple" Background="Transparent"/>
        </Border>

        <Label Grid.Row="4" Style="{StaticResource Body-Label}"
        Content="R E F"></Label>
        <Border Grid.Row="5" >
            <ListBox x:Name="BOM" ItemsSource="{Binding History_Ref}"  SelectionMode="Multiple" Background="Transparent"/>
        </Border>
    </Grid>
</UserControl>

<UserControl x:Class="CadEye.View.Cad_FileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CadEye.View" xmlns:viewcs="clr-namespace:CadEye.ViewCS" d:DataContext="{d:DesignInstance Type=viewcs:Bridge}"
             mc:Ignorable="d" 
            d:DesignHeight="500" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="5"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="1" Background="#444444" CornerRadius="5" HorizontalAlignment="Stretch" Height="25" Padding="0">
                <Grid Margin="5,0,5,0">
                    <TextBox
                        x:Name="Tag_SearchText"
                        Text="{Binding TagText, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource SearchTextBoxStyle}">
                        <TextBox.InputBindings>
                            <KeyBinding 
                                Key="Enter" 
                                Command="{Binding Tag_Enter_Search_Command}" 
                                CommandParameter="{Binding TagText}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                </Grid>
            </Border>

            <Border Grid.Row="3" Background="#444444" CornerRadius="5" HorizontalAlignment="Stretch" Height="25" Padding="0">
                <Grid Margin="5,0,5,0">
                    <TextBox 
                        x:Name="File_SearchText" 
                        BorderThickness="0" 
                        Text="{Binding FileText, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource SearchTextBoxStyle}">
                        <TextBox.InputBindings>
                            <KeyBinding 
                                Key="Enter" 
                                Command="{Binding Enter_Search_Command}" 
                                CommandParameter="{Binding FileText}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                </Grid>
            </Border>

            <Border Grid.Row="5" >
                <DataGrid x:Name="Child_Box" 
                    VerticalAlignment="Top"
                    Style="{StaticResource Data_Gaid_Background}"
                    ColumnHeaderStyle ="{StaticResource Data_Grid_ColumnHeader}"
                    CellStyle="{StaticResource Data_Grid_Cell}"
                    ItemsSource="{Binding Files}"
                    SelectedItem="{Binding SelectedFile, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn 
                            Header="No" 
                            Binding="{Binding Key}" 
                            ElementStyle ="{StaticResource Data_ElementStyle}"
                            Width="30"/>
                        <DataGridTextColumn 
                            Header="Event" 
                            Binding="{Binding FilePath}" 
                            ElementStyle ="{StaticResource Data_ElementStyle}"
                            Width="*"/>
                    </DataGrid.Columns>
                    <DataGrid.ContextMenu>
                        <ContextMenu >
                            <MenuItem Header="Open File" 
                                Command="{Binding File_Open_Command}"
                                CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                            <MenuItem Header="Open Folder" 
                                Command="{Binding Folder_Open_Command}"
                                CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                </DataGrid>
            </Border>

        </Grid>
        <Button 
            Grid.Row="3"
            x:Name="DB_Write_btn"
            Visibility="Visible"
            Content="Tree Update" 
            Command="{Binding Tree_Update_Command}"
            Style="{StaticResource Button_Glass}"/>
        <Button 
            Grid.Row="3" 
            x:Name="DB_Read_btn" 
            Visibility="Hidden" 
            Content="Tree Update"
            Command="{Binding DB_Read_Command}"
            Style="{StaticResource Button_Glass}"></Button>
        <Button 
            Grid.Row="5"
            x:Name="DB_Update_btn" 
            Content="DataBase Update"
            Command="{Binding DB_Update_Command}"
            Style="{StaticResource Button_Glass}"/>
        <Button 
            Grid.Row="7"  
            x:Name="DB_Backup_btn" 
            Content="{Binding BackupButtonText}"
            Command="{Binding BackUp_Command}"
            Style="{StaticResource Button_Glass}"/>
        <Button 
            Grid.Row="9" 
            x:Name="DB_Reset_btn" 
            Content="Reset"
            Command="{Binding Reset_Command}"
            Style="{StaticResource Button_Glass}"/>
    </Grid>
</UserControl>
